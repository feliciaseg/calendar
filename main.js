/** Function to run on window load */
window.addEventListener("load", main)

/** Functions to run on window load */
function main() {
    calendarMain();
    timeDateMain();
    todoMain();
    notificationMain();
}
